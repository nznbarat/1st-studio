# 1st Studio — Видео → 3D render

Хавтас дүүрэн клипийг нэг ижил „3D render" промтоор дамжуулж, **эх нэрээр нь**
буцааж хадгалдаг багц хөрвүүлэгч. Нэг ч суулгац шаардахгүй — `camera.html` шиг
хөтчөөр `video3d.html` нээхэд шууд ажиллана.

**Гурван файл хамт байх ёстой:** `video3d.html` · `video3d.js` · `video3d-styles.js`

---

## 1. Хоёр эх үүсвэр

| | **Локал ComfyUI** | **fal.ai** |
|---|---|---|
| Өртөг | **төлбөргүй, хязгааргүй** | секунд тутам ≈ $0.28 |
| Хэрэгтэй | NVIDIA GPU 12GB+, ComfyUI | API түлхүүр |
| Хурд | клип тутам 3–10 минут | 1–3 минут |
| Чанар | Wan VACE | Seedance 2.5 (илүү) |

Дээд талын таб дээр сонгоно. Тохиргоо бүр хөтөч дээр хадгалагдана.

---

## 2. Хурдан эхлэл — локал ComfyUI

### 2.1 ComfyUI-г **CORS зөвшөөрөлтэй** асаах ⚠

Энэ бол хамгийн түгээмэл саад. Хөтчийн хуудас өөр порт руу хандах тул
ComfyUI зөвшөөрлийн толгой илгээх ёстой:

```bash
python main.py --enable-cors-header
```

Windows portable дээр `run_nvidia_gpu.bat`-ыг засаж мөрийн төгсгөлд
`--enable-cors-header` нэмнэ.

### 2.2 Хуудсаа нээх

ZIP татаад `video3d.html`-ээ давхар товшино (гурван файл хамт байх ёстой).
Эсвэл сайтаас: `/video3d.html`.

> **Анхаар:** `https://` сайтаас локал `http://127.0.0.1` руу хандахыг зарим
> хөтөч хориглодог. ZIP татаад **файлаар нээх** нь хамгийн найдвартай.

### 2.3 Холбогдох

1. Зүүн самбар → **Холболт шалгах**. GPU-гийн нэр гарвал холбогдсон.
2. **Workflow ачаалах** → ComfyUI-гаасаа `Export (API)`-аар гаргасан JSON.
   (Дэлгэрэнгүй: [`video-to-3d/SETUP.md`](video-to-3d/SETUP.md) 7-р алхам)
3. Доор нь видео, промт, seed, кадрын тоо **хаана суухыг** автоматаар олоод
   ногоон өнгөөр харуулна.
4. **Workflow-г сервертэй тулгах** → дутуу зангилаа, байхгүй моделийг хэлнэ.

### 2.4 Ажиллуулах

1. Клипүүдээ дунд талбар руу **чирж оруулна** (бүтэн хавтас чирж болно).
2. **Гаралтын хавтас** сонгоно — доорх 4-р хэсгийг заавал уншаарай.
3. Загвараа сонгоод **▶ Эхлүүлэх**.

---

## 3. fal.ai-аар ажиллуулах

1. Таб → **fal.ai**, түлхүүрээ оруулна ([fal.ai](https://fal.ai)).
2. Түлхүүр зөвхөн энэ хөтөч дээр хадгалагдана. Хуудсаа **олон нийтэд бүү тавь** —
   түлхүүр ил болно.
3. Дээд талд ойролцоо өртөг шууд харагдана.

Хөтчийн хувилбар файлыг data URI хэлбэрээр илгээдэг тул **30 секундээс богино,
100 MB-аас бага** клип тохиромжтой.

---

## 4. Гаралтын хавтас — кирилл нэртэй файл ⚠

Хөтөч **татах** горимд латин биш нэрийг хаядаг: `ангилал.mp4` → `download`.
Энэ нь хөтчийн хязгаарлалт, кодын алдаа биш.

**Шийдэл:** зүүн самбар → **Гаралтын хавтас** дарж хавтас сонгоно.
Тэгвэл файл шууд тэр хавтас руу **эх нэрээрээ** бичигдэнэ.
(Chrome / Edge дээр ажиллана.)

Латин биш нэртэй файл байхад хуудас өөрөө шар анхааруулга харуулна.

---

## 5. Загварууд

10 бэлэн загвар: `3D render` · `Pixar` · `Шавар` · `Low-poly` · `Blockout` ·
`Blender EEVEE` · `Unreal` · `Gaussian splat` · `Wireframe` · `Toon`.

Промтууд нь терминалын хувилбартай **нэг эх сурвалжаас** гардаг
(`video-to-3d/v2v3d/styles.json`). Засвар оруулбал:

```bash
npm run build-styles
```

Өөрийн промт бичвэл загварыг орлоно. `@Video1` гэж бичихгүй бол автоматаар
нэмэгдэнэ (локал загварт илгээхдээ энгийн үг болгож хувиргана).

---

## 6. Терминалын хувилбартай ялгаа

| | Хөтөч (`video3d.html`) | Терминал (`video-to-3d/`) |
|---|---|---|
| Суулгац | **шаардахгүй** | Python + ffmpeg |
| Урт видео хэрчих | ❌ (ffmpeg байхгүй) | ✅ автоматаар хэрчиж, буцааж наана |
| Дахин үргэлжлүүлэх | хэсэгчлэн (бэлэн файлыг алгасна) | ✅ `_manifest.json` |
| Зэрэгцүүлэлт | 1–4 | хязгааргүй |
| Тайлан | JSON татна | `_manifest.json` |

**30 секундээс урт клип** дээр терминалын хувилбарыг ашиглана уу:

```bash
cd video-to-3d
python -m v2v3d run --provider comfy -i ~/videos --workflow миний.json
```

---

## 7. Түгээмэл алдаа

| Шинж тэмдэг | Шалтгаан / шийдэл |
|---|---|
| `Failed to fetch` / холболт тасарсан | ComfyUI-г `--enable-cors-header` тугтай асаагаагүй |
| Хуудас `https://`-ээс холбогдохгүй | ZIP татаад файлаар нээх, эсвэл `http://localhost` дээрээс |
| Гаралт `download` нэртэй | 4-р хэсэг — Гаралтын хавтас сонгоно |
| `'...' зангилаа сервер дээр алга` | **Workflow-г сервертэй тулгах** дарж дутууг харна |
| Промт нөлөөлөхгүй байна | Workflow-гийн `prompt` цэг зөв олдсон эсэхийг зүүн самбараас шалга |
| `CUDA out of memory` | Нягтралаа 480p болгох, **Кадрын дээд**-ийг багасгах |
| Урт клип таслагдаж байна | **Кадрын дээд** хязгаараас хэтэрсэн — терминалын хувилбарыг ашигла |

---

## 8. Хөгжүүлэгчид

```bash
npm test          # 33 тест — хөтчийн логик Python хувилбартай таарч байгааг шалгана
npm run test:e2e  # жинхэнэ Chromium + хуурамч ComfyUI сервер (playwright хэрэгтэй)
npm run build-styles
```

`npm test` нь workflow задлан шинжлэлт, кадрын тоо (4n+1), хэмжээ (16-д
хуваагдах), промтын боловсруулалт зэргийг Python дахь `v2v3d`-тэй **мөр мөрөөр**
тулгана — хоёр хувилбар салж явахаас сэргийлнэ.
